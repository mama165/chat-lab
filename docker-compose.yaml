services:
  ai-gen:
    build:
      context: ./ai
      dockerfile: Dockerfile
    image: chat-lab-ai-gen
    volumes:
      - ./ai:/app
    # Utilisation des variables passées par le Makefile
    user: "${D_UID}:${D_GID}"
    environment:
      - PYTHONUNBUFFERED=1