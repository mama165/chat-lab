# ==============================================================================
# CONFIGURATION
# ==============================================================================

# Server & Network
HOST=localhost
MASTER_PORT=8080
SCANNER_PORT=8081
DEBUG_PORT=8090
LOG_LEVEL=DEBUG
AUTH_TOKEN_DURATION=24h
AUTHENTICATION_ENABLED=false

# Infrastructure (Databases)
BADGER_FILEPATH=/tmp/database/badger/dev
BLUGE_FILEPATH=/tmp/database/bluge/dev

# Pipeline & Buffers
BUFFER_SIZE=20000
CONNECTION_BUFFER_SIZE=1000
NUMBER_OF_WORKERS=10
LIMIT_MESSAGES=5

# Timeouts & Intervals
SINK_TIMEOUT=30s
METRIC_INTERVAL=2s
RESTART_INTERVAL=200ms
INGESTION_TIMEOUT=5s
BUFFER_TIMEOUT=500ms
OBSERVABILITY_INTERVAL=1s
LATENCY_THRESHOLD=500ms

# Monitoring & Backpressure
LOW_CAPACITY_THRESHOLD=50

# Content Analysis Rules
CHARACTER_REPLACEMENT=*
MAX_CONTENT_LENGTH=1000
MIN_SCORING=0.4
MAX_SCORING=0.6
MAX_ANALYZED_EVENT=500

# ------------------------------------------------------------------------------
# Specialist Trio (Toxicity, Sentiment, Business)
# ------------------------------------------------------------------------------
ENABLE_SPECIALISTS=true
MAX_SPECIALIST_BOOT_DURATION=60s
SPECIALIST_TIMEOUT=30s

TOXICITY_BIN_PATH=./bin/toxicity
TOXICITY_PORT=50051

SENTIMENT_BIN_PATH=./bin/sentiment
SENTIMENT_PORT=50052

# ------------------------------------------------------------------------------
# File Scanner & Backpressure
# ------------------------------------------------------------------------------
ROOT_DIR=C:\Users\Maël\Desktop\develop\chat-lab\e2e\testdata\
SCANNER_WORKER_NB=2
DOWNLOADER_WORKER_NB=40
FILE_TRANSFER_INTERVAL=30s
PENDING_FILE_BATCH_SIZE=5
DRIVE_ID=os-main
CHUNK_SIZE_KB=64
MAX_FILE_SIZE_MB=100
TMP_DOWNLOADING_DIR=to_be_analyzed
PROGRESS_LOG_INTERVAL=15s
MAX_TMP_FILE_TO_PROCESS=50
FILE_TMP_JOB_INTERVAL=5s

# Thresholds for scanner throttling (in percent of fileChan capacity)
SCANNER_BACKPRESSURE_LOW_THRESHOLD_PERCENT=70
SCANNER_BACKPRESSURE_HARD_THRESHOLD_PERCENT=90